class CustomFusion:
  @staticmethod
  def fuse(nodes):
    print(f"=== {__class__.__name__} starts to fuse {len(nodes)} nodes ===")
    print(f"=== {__class__.__name__} finishes to fuse {len(nodes)} nodes ===")
    return nodes
