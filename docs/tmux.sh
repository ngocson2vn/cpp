# Create new session
tmux new -s SESSION_NAME

# Detach
Ctr + b, then `d`

# List sessions
tmux ls

# Attach
tmux a
tmux a -t SESSION_NAME

# Split pane horizontally
Ctr + b, then `"`

# Split pane vertically
Ctr + b, then `%`
