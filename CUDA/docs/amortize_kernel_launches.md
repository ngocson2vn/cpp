# Amortize Kernel Launches
To "amortize" in the context of CUDA kernel launches means **spreading a fixed, one-time setup cost (the launch overhead) over a large amount of useful work so that its performance impact becomes negligible**.
